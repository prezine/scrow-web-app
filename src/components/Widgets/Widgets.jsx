import React from 'react'
import TemplatePage from '../Elements/Wraps/TemplatePage'

const Widgets = () => {
  return (
    <TemplatePage hasButton={true} headerTitle={'Widgets'} headerDescription={'Create, Manage, and Build with Pandascrow'} >Widgets</TemplatePage>
  )
}

export default Widgets